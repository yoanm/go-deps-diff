coverage:
    range: "80...100"

flags:
    nightly:
        joined: false

comment:
    show_carryforward_flags: true

github_checks:
    annotations: true

ignore:
  - "shared_test/**" # Not covered as for test purpose only
